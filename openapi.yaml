# Generated with protoc-gen-openapi
# https://github.com/google/gnostic/tree/master/cmd/protoc-gen-openapi

openapi: 3.0.3
info:
    title: CaseService API
    version: 0.0.1
paths:
    /case:
        put:
            tags:
                - CaseService
            operationId: CaseService_UpdateCase
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/UpdateCaseRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/UpdateCaseReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /case/delete:
        put:
            tags:
                - CaseService
            operationId: CaseService_DeleteCasesByCaseIds
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/DeleteCaseByCaseIdsRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DeleteCaseByCaseIdsReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
    /case/{problemId}:
        get:
            tags:
                - CaseService
            operationId: CaseService_GetCases
            parameters:
                - name: problemId
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/GetCasesReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
        post:
            tags:
                - CaseService
            operationId: CaseService_CreateCases
            parameters:
                - name: problemId
                  in: path
                  required: true
                  schema:
                    type: string
            requestBody:
                content:
                    application/json:
                        schema:
                            $ref: '#/components/schemas/CreateCasesRequest'
                required: true
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/CreateCasesReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
        delete:
            tags:
                - CaseService
            operationId: CaseService_DeleteCasesByProblemId
            parameters:
                - name: problemId
                  in: path
                  required: true
                  schema:
                    type: string
            responses:
                "200":
                    description: OK
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/DeleteCasesByProblemIdReply'
                default:
                    description: Default error response
                    content:
                        application/json:
                            schema:
                                $ref: '#/components/schemas/Status'
components:
    schemas:
        Case:
            type: object
            properties:
                id:
                    type: string
                point:
                    type: string
                index:
                    type: string
                isAuto:
                    type: boolean
        CreateCasesReply:
            type: object
            properties:
                ids:
                    type: array
                    items:
                        type: string
        CreateCasesRequest:
            type: object
            properties:
                problemId:
                    type: string
                requests:
                    type: array
                    items:
                        $ref: '#/components/schemas/Case'
        DeleteCaseByCaseIdsReply:
            type: object
            properties: {}
        DeleteCaseByCaseIdsRequest:
            type: object
            properties:
                ids:
                    type: array
                    items:
                        type: string
        DeleteCasesByProblemIdReply:
            type: object
            properties: {}
        GetCasesReply:
            type: object
            properties:
                results:
                    type: array
                    items:
                        $ref: '#/components/schemas/Case'
        GoogleProtobufAny:
            type: object
            properties:
                '@type':
                    type: string
                    description: The type of the serialized message.
            additionalProperties: true
            description: Contains an arbitrary serialized message along with a @type that describes the type of the serialized message.
        Status:
            type: object
            properties:
                code:
                    type: integer
                    description: The status code, which should be an enum value of [google.rpc.Code][google.rpc.Code].
                    format: int32
                message:
                    type: string
                    description: A developer-facing error message, which should be in English. Any user-facing error message should be localized and sent in the [google.rpc.Status.details][google.rpc.Status.details] field, or localized by the client.
                details:
                    type: array
                    items:
                        $ref: '#/components/schemas/GoogleProtobufAny'
                    description: A list of messages that carry the error details.  There is a common set of message types for APIs to use.
            description: 'The `Status` type defines a logical error model that is suitable for different programming environments, including REST APIs and RPC APIs. It is used by [gRPC](https://github.com/grpc). Each `Status` message contains three pieces of data: error code, error message, and error details. You can find out more about this error model and how to work with it in the [API Design Guide](https://cloud.google.com/apis/design/errors).'
        UpdateCaseReply:
            type: object
            properties: {}
        UpdateCaseRequest:
            type: object
            properties:
                id:
                    type: string
                point:
                    type: string
                index:
                    type: string
                isAuto:
                    type: boolean
                problemId:
                    type: string
tags:
    - name: CaseService
